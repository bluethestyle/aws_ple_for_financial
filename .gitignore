# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
*.whl

# Virtual environments
.venv/
venv/
env/
.env
.env.*
!.env.example

# AWS credentials (NEVER commit these)
.aws/
*.pem
*.key

# Jupyter
.ipynb_checkpoints/
*.ipynb

# ML artifacts
*.pkl
*.pt
*.pth
*.ckpt
*.h5
*.bin
mlruns/
outputs/

# Data (exclude core/data/ which is source code)
data/
!core/data/
*.parquet
*.csv
*.jsonl
!examples/**/sample_data/*.csv

# Logs
logs/
*.log

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# CDK
cdk.out/
.cdk.staging/

# Pytest / testing / linting caches
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Orchestrator state (crash recovery)
.ablation_state.json
outreach/

# Personal resume / CV / LinkedIn exports (local only)
resume/

# Typst compile / auxiliary artifacts
*.pdf.bak
typst-package/

# Print edition scaffold (personal-use commemorative prints, not part of research artifact)
paper/typst/print/

# Stray draft files at repository root
# (place drafts in docs/design/ or paper/, not in repo root)
/*.typ

# Local backup / editor junk
*.bak
*~
*.orig
*.swo

# Node (if ever added)
node_modules/
.codebuild_tmp/

# Local artifact / debug dirs (Phase 0 / Mamba / CodeBuild)
.phase0_artifacts/
.phase0_audit_v2/
.phase0_audit_v3/
.mamba_artifacts/
.codebuild_tmp/
phase0_*.txt
